# Master password for the single-user login page
MASTER_PASSWORD=your-strong-password-here

# Secret key for signing session tokens — generate with: openssl rand -base64 32
SESSION_SECRET=your-random-secret-here
